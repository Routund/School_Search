Search engine for files in the school system

REQUIRED PYTHON PACKAGES:
nltk
pypdf
bs4

wordnet dataset needs to be downloaded to tokenize files (~37 MB).
to download, uncomment the command:

    # import nltk
    # nltk.download('wordnet')

in the crawler.py file the first time the website is run. You may comment it out in later runs.